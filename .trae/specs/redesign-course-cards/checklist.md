# 数据分析课程卡片重新设计 - 验证检查清单

## 类型定义验证
- [ ] 检查是否在 `types/index.ts` 中添加了 `Module` 接口
- [ ] 检查 `Course` 接口是否添加了可选的 `modules` 字段
- [ ] 运行 TypeScript 编译，确保没有类型错误

## 课程数据验证
- [ ] 检查 Python 数据分析基础（课程1）是否有3个模块：Python基础语法(5节)、Pandas数据处理(5节)、数据清洗实战(5节)
- [ ] 检查 Excel 数据处理与分析（课程2）是否有3个模块：基础函数(4节)、数据透视表(4节)、高级分析(4节)
- [ ] 检查 数据可视化实战（课程3）是否有3个模块：Matplotlib基础(6节)、Seaborn进阶(6节)、业务图表实战(6节)
- [ ] 检查 统计分析与假设检验（课程4）是否有3个模块：统计基础(7节)、描述性统计(7节)、假设检验实战(7节)
- [ ] 检查 机器学习入门（课程5）是否有3个模块：算法基础(8节)、分类与回归(8节)、项目实战(8节)
- [ ] 检查每门课程的封面图是否与课程主题相关
- [ ] 检查其他课程（6、7号）的数据是否保持不变

## 课程卡片UI验证
- [ ] 检查课程卡片的信息层级是否正确：课程名称 > 阶段标签 > 模块列表 > 课时数据
- [ ] 检查课程卡片是否清晰显示模块列表，每个模块标注名称和课时数
- [ ] 检查设计风格是否统一，配色是否干净清爽
- [ ] 检查非目标课程（6、7号）的显示方式是否保持不变
- [ ] 检查进度条、搜索、筛选等现有功能是否正常工作

## 响应式设计验证
- [ ] 在桌面端（1024px+）测试课程卡片显示
- [ ] 在平板端（768px-1023px）测试课程卡片显示
- [ ] 在移动端（<768px）测试课程卡片显示
- [ ] 检查在不同屏幕尺寸下布局是否合理，信息是否完整显示

## 浏览器兼容性
- [ ] 在 Chrome/Edge 中测试课程卡片
- [ ] 在 Firefox 中测试课程卡片
- [ ] 在 Safari 中测试课程卡片（如果可能）
