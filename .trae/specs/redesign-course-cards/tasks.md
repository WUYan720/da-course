# 数据分析课程卡片重新设计 - 实现计划

## [ ] 任务 1: 更新类型定义
- **优先级**: P0
- **依赖**: 无
- **描述**: 更新 `Course` 接口，添加 `modules` 字段
  - 在 `/workspace/src/types/index.ts` 中添加 `Module` 接口
  - 更新 `Course` 接口，添加 `modules?: Module[]` 字段
  - 确保向后兼容（使用可选字段）
- **验收标准覆盖**: AC-1
- **测试要求**:
  - `programmatic` TR-1.1: 类型定义编译通过，无TypeScript错误
  - `human-judgment` TR-1.2: 检查类型定义的完整性和合理性
- **备注**: 模块结构应该包含：name（模块名称）、lessonsCount（课时数）

## [ ] 任务 2: 更新课程数据
- **优先级**: P0
- **依赖**: 任务1
- **描述**: 更新5门目标课程的数据，添加完整的模块信息
  - 课程1（Python 数据分析基础）：模块1 "Python基础语法"（5节）、模块2 "Pandas数据处理"（5节）、模块3 "数据清洗实战"（5节）
  - 课程2（Excel 数据处理与分析）：模块1 "基础函数"（4节）、模块2 "数据透视表"（4节）、模块3 "高级分析"（4节）
  - 课程3（数据可视化实战）：模块1 "Matplotlib基础"（6节）、模块2 "Seaborn进阶"（6节）、模块3 "业务图表实战"（6节）
  - 课程4（统计分析与假设检验）：模块1 "统计基础"（7节）、模块2 "描述性统计"（7节）、模块3 "假设检验实战"（7节）
  - 课程5（机器学习入门）：模块1 "算法基础"（8节）、模块2 "分类与回归"（8节）、模块3 "项目实战"（8节）
  - 为每门课程更新更主题相关的封面图URL
- **验收标准覆盖**: AC-1, AC-4
- **测试要求**:
  - `programmatic` TR-2.1: 课程数据格式正确，所有5门课程都有modules字段
  - `human-judgment` TR-2.2: 检查模块名称、课时数是否与需求一致
  - `human-judgment` TR-2.3: 检查封面图是否与课程主题相关
- **备注**: 保持现有课程内容（lessons）不变

## [ ] 任务 3: 重新设计课程卡片组件
- **优先级**: P0
- **依赖**: 任务2
- **描述**: 更新 `/workspace/src/pages/Courses.tsx` 中的课程卡片UI
  - 优化信息层级：课程名称 > 阶段标签 > 模块列表 > 课时数据
  - 为有模块信息的课程显示模块列表
  - 保持现有功能（进度条、搜索、筛选）
  - 使用干净清爽的配色方案
  - 确保响应式设计
- **验收标准覆盖**: AC-2, AC-3, AC-4, AC-5
- **测试要求**:
  - `human-judgment` TR-3.1: 检查课程卡片是否正确显示模块信息
  - `human-judgment` TR-3.2: 检查信息层级是否符合要求
  - `human-judgment` TR-3.3: 检查设计风格是否统一、干净
  - `human-judgment` TR-3.4: 在不同屏幕尺寸下测试响应式布局
  - `programmatic` TR-3.5: 确保现有功能（进度条、搜索、筛选）正常工作
- **备注**: 保持非目标课程（6号和7号）的现有显示方式

## 模块接口设计
```typescript
interface Module {
  name: string;
  lessonsCount: number;
}
```
